what is constructor?
__construct is a magic method in PHP that is used as a constructor for a class. It is called automatically when an object of the class is created. In Laravel, __construct is used to define the dependencies that a controller needs to function properly.
